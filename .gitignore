node_modules/
dist/
.env
*.log
.DS_Store

# build/dev artifacts and large sources not needed to build or deploy
*.zip
replica-*.png
preview*.png
pv*.png
loc*.png
mp*.png
club*.png
land*.png
band.png
amen.png
gal.png
hero*.png
fit*.png
wm.png
# anchored to repo root so client/src/data/ is NOT ignored
/_backups/
/_original_reference/
/shots/
.playwright-mcp/
/data/
/data2/
/revert-theme.sh
server/assets/*.pdf

# staging folder for supplied renders — copied into images/ by hand, and
# everything under public/ ships in the build, so keep it out of the repo
/client/public/NEW/
